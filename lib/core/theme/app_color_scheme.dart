import 'package:flutter/material.dart';

abstract class AppColorScheme {
  Color get primary;
  Color get onPrimary;
  Color get primaryContainer;
  Color get secondary;
  Color get surface;
  Color get error;
} 